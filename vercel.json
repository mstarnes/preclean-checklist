{
  "framework": null,
  "buildCommand": "cd frontend && yarn build && rm -rf api/public && mv frontend/build api/public",
  "installCommand": "cd frontend && yarn install && cd .. && cd api && yarn install",
  "ignoreCommand": "exit 1",
  "builds": [
    {
      "src": "api/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/server.js"
    }
  ]
}