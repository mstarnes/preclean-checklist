{
  "framework": null,
  "ignoreCommand": "exit 1",
  "buildCommand": "cd frontend && yarn build",
  "installCommand": "cd frontend && yarn install && cd .. && cd api && npm install",
  "ignoreCommand": "exit 1",
  "routes": [
    { "src": "/(.*)", "dest": "/api/server.js" }
  ],
  "builds": [
    { "src": "api/server.js", "use": "@vercel/node" }
  ]
}