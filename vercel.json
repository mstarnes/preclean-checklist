{
  "framework": null,
  "buildCommand": "cd frontend && yarn build && cp -r frontend/build/* .",
  "installCommand": "cd frontend && yarn install && cd .. && cd api && yarn install",
  "ignoreCommand": "exit 1",
  "builds": [
    {
      "src": "api/server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.js"
    },
    {
      "source": "/auth/google(.*)",
      "destination": "/api/server.js"
    },
    {
      "source": "/refresh",
      "destination": "/api/server.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}