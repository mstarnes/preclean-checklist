{
  "framework": null,
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "cd api && npm install && cd .. && cd frontend && npm install",
  "ignoreCommand": "exit 1",
  "routes": [
    { "src": "/(.*)", "dest": "/api/server.js" }
  ],
  "builds": [
    { "src": "api/server.js", "use": "@vercel/node" }
  ]
}